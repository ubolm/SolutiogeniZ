import { CrmSystemLoading } from "@/components/crm/CrmSystemLoading";

export default function CrmLoadingPage() {
  return (
    <CrmSystemLoading
      subtitle="Conectando paneles, reglas y accesos del equipo."
      title="Preparando entorno"
    />
  );
}
